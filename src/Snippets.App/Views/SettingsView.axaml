<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:Snippets.App.ViewModels"
             xmlns:ic="using:FluentIcons.Avalonia.Fluent"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="500"
             x:Class="Snippets.App.Views.SettingsView"
             x:DataType="vm:SettingsViewModel">

  <Grid Margin="24" RowDefinitions="Auto,Auto,Auto,*">
    <TextBlock Grid.Row="0" FontSize="28" FontWeight="SemiBold" Text="Settings" />
    <TextBlock Grid.Row="1" Margin="0,4,0,0" Opacity="0.7" TextWrapping="Wrap"
               Text="Control startup and tray behavior for this app." />

    <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,16,0,0"
                VerticalAlignment="Center">
      <Button Click="OnOpenConfigFolderClick" ToolTip.Tip="Open config folder">
        <ic:SymbolIcon Symbol="FolderOpen" FontSize="14" />
      </Button>
      <TextBlock Text="{Binding StatusText}"
                 IsVisible="{Binding HasStatus}"
                 VerticalAlignment="Center"
                 Opacity="0.65"
                 FontSize="12"
                 TextWrapping="Wrap" />
    </StackPanel>

    <StackPanel Grid.Row="3" Margin="0,20,0,0" Spacing="10">
      <Border Padding="16"
              Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
              CornerRadius="6" BorderThickness="1"
              BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
        <Grid ColumnDefinitions="*,Auto">
          <StackPanel Spacing="4">
            <TextBlock Text="Start with system" FontSize="16" FontWeight="SemiBold" />
            <TextBlock Text="Register Snippets in the current user's Windows startup items and start it minimized to the tray."
                       Opacity="0.68" TextWrapping="Wrap" />
          </StackPanel>
          <ToggleSwitch Grid.Column="1"
                        VerticalAlignment="Center"
                        IsEnabled="{Binding !IsSaving}"
                        IsChecked="{Binding IsStartWithSystemEnabled}"
                        OnContent="On"
                        OffContent="Off"
                        Click="OnStartWithSystemClick" />
        </Grid>
      </Border>

      <Border Padding="16"
              Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
              CornerRadius="6" BorderThickness="1"
              BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
        <Grid ColumnDefinitions="*,Auto">
          <StackPanel Spacing="4">
            <TextBlock Text="Keep running in system tray" FontSize="16" FontWeight="SemiBold" />
            <TextBlock Text="When the window closes, keep Snippets in the tray instead of quitting."
                       Opacity="0.68" TextWrapping="Wrap" />
          </StackPanel>
          <ToggleSwitch Grid.Column="1"
                        VerticalAlignment="Center"
                        IsEnabled="{Binding !IsSaving}"
                        IsChecked="{Binding IsCloseToTrayEnabled}"
                        OnContent="On"
                        OffContent="Off"
                        Click="OnTrayClick" />
        </Grid>
      </Border>

      <Border Padding="16"
              Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
              CornerRadius="6" BorderThickness="1"
              BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
        <StackPanel Spacing="4">
          <TextBlock Text="System IP addresses" FontSize="16" FontWeight="SemiBold" />
          <TextBlock Text="Active local IPv4 addresses on this device."
                     Opacity="0.68" TextWrapping="Wrap" />
          <TextBlock Text="{Binding SystemIpAddresses}"
                     Margin="0,4,0,0"
                     FontFamily="{StaticResource MonoFont}"
                     TextWrapping="Wrap" />
        </StackPanel>
      </Border>
    </StackPanel>

  </Grid>
</UserControl>
